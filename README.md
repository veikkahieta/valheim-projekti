# valheim-projekti
Miniprojekti h6 Tero Karvisen Palvelinten hallinta -kurssille 2025.<br>

Tavoitteena konfiguroida ja luoda pelipalvelin Valheimiin.

```
sudo apt-get update
sudo apt install -y curl wget vim tar gzip lib32gcc-s1 lib32stdc++6

sudo useradd -m steam
sudo passwd steam

sudo su - steam

```
